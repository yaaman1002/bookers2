部分テンプレート

<h1>Books</h1>
    <div class="col-lg-8">
         <table class="table table-striped">
         <thead>
         <tr>
          <th></th>
          <th>Title</th>
          <th>Opinion</th>
          <th colspan="3"></th>
         </tr>
        <tbody>
        <% @user.each do |user| %>
          <tr>
            <td><%= image_tag user.book.image.get_image %></td>
            <td><%= link_to books_path(@book.id), user.book.title %></td>
            <td><%= user.book.body %></td>
         </tr>
        <% end %>
        </tbody>
    </thead>
 </table>
</div>
